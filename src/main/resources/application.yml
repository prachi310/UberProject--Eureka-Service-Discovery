
spring:
  application:
    name: UberProject-ServiceDiscovery-EurekaServer

server:
  port: 8761

eureka:
  instance:
    hostname: my-registry
  client:
    register-with-eureka: false
    fetch-registry: false

